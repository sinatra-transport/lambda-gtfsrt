{
  "name": "gtfsrt-lambda",
  "version": "0.0.1",
  "description": "A lambda to scrap a GTFS-RT config and generate smaller files for each route, trip, and stop",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "build": "tsc && mkdir ./dist/src/proto && cp ./src/proto/*.js ./dist/src/proto",
    "test": "tsx test.ts"
  },
  "author": "Emily McLean",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.149",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=22.0.0 <23.0.0"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.812.0",
    "aws-sdk": "^2.1692.0",
    "got": "^14.4.7",
    "just-group-by": "^2.2.0",
    "protobufjs": "^7.5.2"
  }
}
